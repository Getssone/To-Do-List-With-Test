{% extends 'base-old.html.twig' %}

{% block body %}
	<div class="row">
		{{ form_start(form, {'action' : path('edit.task-old', { 'id' : task.id })}) }}
		{{ form_widget(form) }}

		<button type="submit" class="btn btn-success pull-right">Modifier</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
