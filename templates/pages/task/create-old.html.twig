{% extends 'base-old.html.twig' %}

{% block body %}
	<div class="row">
		{{ form_start(form, {'action' : path('new.task-old')}) }}
		{{ form_widget(form) }}

		<button type="submit" class="btn btn-success pull-right">Ajouter</button>
		{{ form_end(form) }}

		<a href="{{ path('list.task-old') }}" class="btn btn-primary">Retour à la liste des tâches</a>
	</div>
{% endblock %}
