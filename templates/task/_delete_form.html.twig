<form method="post" action="{{ path('deleted.task', {'id': task.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
	<button class="btn btn-danger btn-sm pull-right">{{ button_label|default('Supression') }}</button>
</form>
